id: reliable_reporting_reconciled_billing
version: "1.0.0"
introduced_in: "3.2"
title: "Reliable Reporting: Reconciled Billing"
category: capability_discovery
summary: "Validates the Reconciled Billing tier's canonical evidence and authenticated receipt path for official revisions and later adjustments."
track: reporting
required_tools:
  - get_adcp_capabilities
  - get_reporting_status
  - sync_reporting_receipts

requires:
  - controller

requires_all_capabilities:
  - path: "media_buy.reporting_delivery.reliable_reporting_version"
    equals: "1.0"
  - path: "media_buy.reporting_delivery.reconciled_billing"
    equals: true

narrative: |
  Reconciled Billing is an explicit Reliable Reporting tier, not a synonym for
  a seller exporting invoice data. Its capability must select a
  consumer_receipt offering with canonicalization evidence and must expose the
  receipt task. That same task records accepted or rejected receipts for
  post-official adjustments without rewriting the original reporting revision.

agent:
  interaction_model: any
  capabilities:
    - "media_buy.reporting_delivery"
  examples:
    - "Seller agent supporting bilateral billing reconciliation"

caller:
  role: buyer_agent
  example: "Compliance harness acting as an authorized billing consumer"

prerequisites:
  description: |
    The seller advertises Reliable Reporting 1.0 and reconciled_billing: true.
  test_kit: "test-kits/acme-outdoor.yaml"

phases:
  - id: reconciled_billing_declaration
    title: "Discover Reconciled Billing guarantees"
    steps:
      - id: get_reliable_reporting_reconciled_capabilities
        title: "Read the Reconciled Billing declaration"
        task: get_adcp_capabilities
        schema_ref: "protocol/get-adcp-capabilities-request.json"
        response_schema_ref: "protocol/get-adcp-capabilities-response.json"
        doc_ref: "/protocol/get_adcp_capabilities"
        stateful: false
        sample_request:
          context:
            correlation_id: "reliable_reporting_reconciled_billing--get_capabilities"
        validations:
          - check: response_schema
            description: "Capability response enforces a consumer_receipt offering with canonicalization evidence"
          - check: field_value
            path: "media_buy.reporting_delivery.reliable_reporting_version"
            value: "1.0"
            description: "Seller explicitly claims Reliable Reporting 1.0"
          - check: field_value
            path: "media_buy.reporting_delivery.reconciled_billing"
            value: true
            description: "Reconciled Billing tier is explicitly supported"
          - check: field_value
            path: "media_buy.reporting_delivery.receipt_task"
            value: "sync_reporting_receipts"
            description: "Revision and adjustment reconciliation uses the authenticated receipt task"

  - id: reconciled_billing_lifecycle
    title: "Reconcile official billing evidence and its later correction"
    narrative: |
      The controller seeds a verified official billing revision. The buyer
      stores its checkpoint, accepts the exact revision, then observes a
      post-publication adjustment and submits both accepted and rejected adjustment
      evidence. The final changes_after read must expose the immutable reporting
      anchor, correction, and authenticated outcomes without conflating them.
    steps:
      - id: prepare_reconciled_billing
        title: "Prepare one verified official billing revision"
        task: comply_test_controller
        schema_ref: "compliance/comply-test-controller-request.json"
        response_schema_ref: "compliance/comply-test-controller-response.json"
        comply_scenario: reliable_reporting_reconciled_billing_probe
        stateful: true
        context_outputs:
          - name: reliable_reporting_billing_revision_id
            path: "simulated.reporting_revision_id"
          - name: reliable_reporting_billing_obligation_id
            path: "simulated.reporting_obligation_id"
          - name: reliable_reporting_billing_materialization_id
            path: "simulated.reporting_materialization_id"
        sample_request:
          account:
            account_id: "reporting_core_lab"
            sandbox: true
          scenario: "reliable_reporting_reconciled_billing_probe"
          params:
            operation: "prepare"
          context:
            correlation_id: "reliable_reporting_billing--prepare"
        validations:
          - check: response_schema
            description: "Controller response is schema-valid"
          - check: field_value
            path: "success"
            value: true
            description: "Official revision and verified materialization were prepared"
          - check: field_present
            path: "simulated.canonical_content_digest"
            description: "Fixture exposes exact canonical billing evidence"

      - id: read_reconciled_billing_baseline
        title: "Read the official anchor and save its checkpoint"
        task: get_reporting_status
        schema_ref: "media-buy/get-reporting-status-request.json"
        response_schema_ref: "media-buy/get-reporting-status-response.json"
        doc_ref: "/media-buy/task-reference/get_reporting_status"
        comply_scenario: reliable_reporting_reconciled_billing_probe
        stateful: true
        context_outputs:
          - name: reliable_reporting_billing_checkpoint
            path: "changes_checkpoint"
        sample_request:
          account:
            account_id: "reporting_core_lab"
          view: "periods"
          context:
            correlation_id: "reliable_reporting_billing--baseline"
        validations:
          - check: response_schema
            description: "Billing ledger response is schema-valid"
          - check: field_equals_context
            path: "revisions[0].reporting_revision_id"
            context_key: "reliable_reporting_billing_revision_id"
            description: "Billing evidence binds to the seeded official revision"
          - check: field_value
            path: "revisions[0].finality"
            value: "official"
            description: "Reconciliation starts from a terminal close"
          - check: field_present
            path: "changes_checkpoint"
            description: "Buyer saves a durable pre-correction checkpoint"
          - check: field_present
            path: "adjustment_receipts"
            description: "Reconciled Billing exposes the authenticated correction outcomes collection"
          - check: field_value
            path: "periods[0].health"
            value: "action_required"
            description: "Readable evidence with a missing current receipt is immediately action_required, without an invented SLA"
          - check: field_value
            path: "periods[0].issues[0].code"
            value: "RECEIPT_REQUIRED"
            description: "The missing revision receipt is a structured buyer action"

      - id: reject_official_revision
        title: "Record a rejected official-materialization receipt"
        task: sync_reporting_receipts
        schema_ref: "media-buy/sync-reporting-receipts-request.json"
        response_schema_ref: "media-buy/sync-reporting-receipts-response.json"
        doc_ref: "/media-buy/task-reference/sync_reporting_receipts"
        comply_scenario: reliable_reporting_reconciled_billing_probe
        stateful: true
        sample_request:
          account:
            account_id: "reporting_core_lab"
          idempotency_key: "$generate:uuid_v4#reliable_reporting_billing_official_receipt"
          receipts:
            - reporting_receipt_id: "rr-billing-official-receipt-0001"
              reporting_obligation_id: "$context.reliable_reporting_billing_obligation_id"
              reporting_revision_id: "$context.reliable_reporting_billing_revision_id"
              reporting_materialization_id: "$context.reliable_reporting_billing_materialization_id"
              status: "rejected"
              verification_profile: "canonical_digest"
              observed_row_count: 2
              observed_control_totals:
                - name: "impressions"
                  value: "5"
                  value_type: "integer"
                  unit: "impressions"
                - name: "spend"
                  value: "8.00"
                  value_type: "decimal"
                  unit: "USD"
              rejection_codes: ["CANONICAL_DIGEST_MISMATCH"]
              observed_at: "2026-08-27T04:01:00Z"
          context:
            correlation_id: "reliable_reporting_billing--accept-official"
        validations:
          - check: response_schema
            description: "Receipt acknowledgement is schema-valid"
          - check: field_value
            path: "results[0].result"
            value: "recorded"
            description: "Seller durably records the rejected official revision evidence"
          - check: field_equals_context
            path: "results[0].receipt.reporting_revision_id"
            context_key: "reliable_reporting_billing_revision_id"
            description: "Rejected evidence remains bound to the immutable reporting revision"

      - id: supersede_rejected_official_revision_receipt
        title: "Replace the rejected revision receipt with accepted evidence"
        task: sync_reporting_receipts
        schema_ref: "media-buy/sync-reporting-receipts-request.json"
        response_schema_ref: "media-buy/sync-reporting-receipts-response.json"
        doc_ref: "/media-buy/task-reference/sync_reporting_receipts"
        comply_scenario: reliable_reporting_reconciled_billing_probe
        stateful: true
        sample_request:
          account:
            account_id: "reporting_core_lab"
          idempotency_key: "$generate:uuid_v4#reliable_reporting_billing_official_repair"
          receipts:
            - reporting_receipt_id: "rr-billing-official-repaired-0001"
              supersedes_reporting_receipt_id: "rr-billing-official-receipt-0001"
              reporting_obligation_id: "$context.reliable_reporting_billing_obligation_id"
              reporting_revision_id: "$context.reliable_reporting_billing_revision_id"
              reporting_materialization_id: "$context.reliable_reporting_billing_materialization_id"
              status: "accepted"
              verification_profile: "canonical_digest"
              observed_row_count: 2
              observed_control_totals:
                - name: "impressions"
                  value: "5"
                  value_type: "integer"
                  unit: "impressions"
                - name: "spend"
                  value: "8.00"
                  value_type: "decimal"
                  unit: "USD"
              observed_canonical_content_digest:
                algorithm: "sha256"
                value: "bcd079902f3c8edb4315dbbdaf9b4e37f6fd5af33c80d1fe6ac8c655581342d4"
                canonicalization_id: "billing-rows-v1"
                canonicalization_uri: "https://test-agent.adcontextprotocol.org/reporting/canonicalization/billing-rows-v1.json"
                canonicalization_sha256: "f98d55f8b7ad49c058b17f04630576c6b27b3564090b838e49834dd7714cdefa"
              observed_at: "2026-08-27T04:02:00Z"
        validations:
          - check: response_schema
            description: "Revision-replacement acknowledgement is schema-valid"
          - check: field_value
            path: "results[0].result"
            value: "recorded"
            description: "A new immutable accepted receipt replaces only the current rejected leaf"

      - id: reject_terminal_official_revision_receipt
        title: "Reject a stale revision supersession after acceptance"
        task: sync_reporting_receipts
        schema_ref: "media-buy/sync-reporting-receipts-request.json"
        response_schema_ref: "media-buy/sync-reporting-receipts-response.json"
        comply_scenario: reliable_reporting_reconciled_billing_probe
        stateful: true
        sample_request:
          account:
            account_id: "reporting_core_lab"
          idempotency_key: "$generate:uuid_v4#reliable_reporting_billing_official_stale"
          receipts:
            - reporting_receipt_id: "rr-billing-official-stale-0001"
              supersedes_reporting_receipt_id: "rr-billing-official-repaired-0001"
              reporting_obligation_id: "$context.reliable_reporting_billing_obligation_id"
              reporting_revision_id: "$context.reliable_reporting_billing_revision_id"
              reporting_materialization_id: "$context.reliable_reporting_billing_materialization_id"
              status: "accepted"
              verification_profile: "canonical_digest"
              observed_row_count: 2
              observed_control_totals: []
              observed_canonical_content_digest:
                algorithm: "sha256"
                value: "bcd079902f3c8edb4315dbbdaf9b4e37f6fd5af33c80d1fe6ac8c655581342d4"
                canonicalization_id: "billing-rows-v1"
                canonicalization_uri: "https://test-agent.adcontextprotocol.org/reporting/canonicalization/billing-rows-v1.json"
                canonicalization_sha256: "f98d55f8b7ad49c058b17f04630576c6b27b3564090b838e49834dd7714cdefa"
              observed_at: "2026-08-27T04:03:00Z"
        validations:
          - check: response_schema
            description: "Terminal revision rejection response is schema-valid"
          - check: field_value
            path: "results[0].result"
            value: "failed"
            description: "An accepted current revision receipt is terminal"

      - id: read_accepted_revision_receipt
        title: "Confirm acceptance completes the current receipt obligation"
        task: get_reporting_status
        schema_ref: "media-buy/get-reporting-status-request.json"
        response_schema_ref: "media-buy/get-reporting-status-response.json"
        comply_scenario: reliable_reporting_reconciled_billing_probe
        stateful: true
        sample_request:
          account:
            account_id: "reporting_core_lab"
          view: "periods"
        validations:
          - check: response_schema
            description: "Accepted-receipt status response is schema-valid"
          - check: field_value
            path: "periods[0].reconciliation_status"
            value: "accepted"
            description: "The one current accepted revision receipt is terminal"
          - check: field_value
            path: "periods[0].health"
            value: "complete"
            description: "The closed fixture becomes complete after the current receipt is accepted"

      - id: read_revision_receipt_history
        title: "Read back append-only rejected and replacement receipt history"
        task: get_reporting_status
        schema_ref: "media-buy/get-reporting-status-request.json"
        response_schema_ref: "media-buy/get-reporting-status-response.json"
        comply_scenario: reliable_reporting_reconciled_billing_probe
        stateful: true
        sample_request:
          account:
            account_id: "reporting_core_lab"
          view: "revision"
          reporting_revision_id: "$context.reliable_reporting_billing_revision_id"
        validations:
          - check: response_schema
            description: "Revision receipt history response is schema-valid"
          - check: array_length
            path: "receipts"
            value: 2
            description: "Rejected and accepted replacement receipts remain append-only history"

      - id: publish_reconciled_adjustments
        title: "Publish accepted and disputed post-invoice corrections"
        task: comply_test_controller
        schema_ref: "compliance/comply-test-controller-request.json"
        response_schema_ref: "compliance/comply-test-controller-response.json"
        comply_scenario: reliable_reporting_reconciled_billing_probe
        stateful: true
        context_outputs:
          - name: reliable_reporting_accepted_adjustment_id
            path: "simulated.adjustments[0].reporting_adjustment_id"
          - name: reliable_reporting_accepted_adjustment_digest
            path: "simulated.adjustments[0].canonical_adjustment_sha256"
          - name: reliable_reporting_disputed_adjustment_id
            path: "simulated.adjustments[1].reporting_adjustment_id"
          - name: reliable_reporting_disputed_adjustment_digest
            path: "simulated.adjustments[1].canonical_adjustment_sha256"
          - name: reliable_reporting_disputed_observed_digest
            path: "simulated.disputed_observed_adjustment_sha256"
        sample_request:
          account:
            account_id: "reporting_core_lab"
            sandbox: true
          scenario: "reliable_reporting_reconciled_billing_probe"
          params:
            operation: "publish_adjustment"
          context:
            correlation_id: "reliable_reporting_billing--publish-adjustments"
        validations:
          - check: response_schema
            description: "Controller response is schema-valid"
          - check: field_value
            path: "success"
            value: true
            description: "Two immutable post-official adjustments were published"
          - check: field_equals_context
            path: "simulated.adjustments[0].adjusts_reporting_revision_id"
            context_key: "reliable_reporting_billing_revision_id"
            description: "Accepted-path correction targets the immutable revision"
          - check: field_equals_context
            path: "simulated.adjustments[1].adjusts_reporting_revision_id"
            context_key: "reliable_reporting_billing_revision_id"
            description: "Dispute-path correction targets the same immutable revision"
          - check: field_present
            path: "simulated.disputed_observed_adjustment_sha256"
            description: "Controller supplies the buyer-observed mismatching digest used by the dispute path"

      - id: read_adjustment_reopened_state
        title: "Observe a new adjustment reopen reconciliation immediately"
        task: get_reporting_status
        schema_ref: "media-buy/get-reporting-status-request.json"
        response_schema_ref: "media-buy/get-reporting-status-response.json"
        comply_scenario: reliable_reporting_reconciled_billing_probe
        stateful: true
        sample_request:
          account:
            account_id: "reporting_core_lab"
          view: "periods"
        validations:
          - check: response_schema
            description: "Reopened reconciliation response is schema-valid"
          - check: field_value
            path: "periods[0].reconciliation_status"
            value: "pending"
            description: "A new applicable adjustment reopens previously accepted evidence"
          - check: field_value
            path: "periods[0].health"
            value: "action_required"
            description: "Missing adjustment receipts are immediately actionable, not waiting"
          - check: field_value
            path: "periods[0].issues[0].code"
            value: "ADJUSTMENT_RECEIPT_REQUIRED"
            description: "The buyer receives the precise missing-adjustment action"

      - id: reconcile_post_official_adjustments
        title: "Accept one correction and reject the other"
        task: sync_reporting_receipts
        schema_ref: "media-buy/sync-reporting-receipts-request.json"
        response_schema_ref: "media-buy/sync-reporting-receipts-response.json"
        doc_ref: "/media-buy/task-reference/sync_reporting_receipts"
        comply_scenario: reliable_reporting_reconciled_billing_probe
        stateful: true
        sample_request:
          account:
            account_id: "reporting_core_lab"
          idempotency_key: "$generate:uuid_v4#reliable_reporting_billing_adjustment_receipts"
          adjustment_receipts:
            - reporting_receipt_id: "rr-adjustment-accepted-receipt-01"
              reporting_adjustment_id: "$context.reliable_reporting_accepted_adjustment_id"
              adjusts_reporting_revision_id: "$context.reliable_reporting_billing_revision_id"
              status: "accepted"
              observed_adjustment_sha256: "$context.reliable_reporting_accepted_adjustment_digest"
              observed_at: "2026-08-29T10:01:00Z"
            - reporting_receipt_id: "rr-adjustment-rejected-receipt-01"
              reporting_adjustment_id: "$context.reliable_reporting_disputed_adjustment_id"
              adjusts_reporting_revision_id: "$context.reliable_reporting_billing_revision_id"
              status: "rejected"
              observed_adjustment_sha256: "$context.reliable_reporting_disputed_observed_digest"
              rejection_codes: ["CANONICAL_DIGEST_MISMATCH"]
              observed_at: "2026-08-29T10:01:01Z"
          context:
            correlation_id: "reliable_reporting_billing--reconcile-adjustments"
        validations:
          - check: response_schema
            description: "Adjustment receipt acknowledgements are schema-valid"
          - check: field_value
            path: "results[0].result"
            value: "recorded"
            description: "Accepted correction outcome is durable"
          - check: field_value
            path: "results[0].adjustment_receipt.status"
            value: "accepted"
            description: "Exact digest match is accepted"
          - check: field_value
            path: "results[1].adjustment_receipt.status"
            value: "rejected"
            description: "Digest disagreement remains a durable dispute"

      - id: read_rejected_adjustment_state
        title: "Keep rejected current adjustment evidence actionable"
        task: get_reporting_status
        schema_ref: "media-buy/get-reporting-status-request.json"
        response_schema_ref: "media-buy/get-reporting-status-response.json"
        comply_scenario: reliable_reporting_reconciled_billing_probe
        stateful: true
        sample_request:
          account:
            account_id: "reporting_core_lab"
          view: "periods"
        validations:
          - check: response_schema
            description: "Rejected-evidence status response is schema-valid"
          - check: field_value
            path: "periods[0].reconciliation_status"
            value: "rejected"
            description: "A rejected current adjustment receipt preserves the evidence discrepancy"
          - check: field_value
            path: "periods[0].issues[0].code"
            value: "ADJUSTMENT_RECEIPT_REJECTED"
            description: "Rejected evidence assigns seller investigation/remediation"

      - id: repair_reconciled_billing_from_checkpoint
        title: "Re-read every correction outcome from the old checkpoint"
        task: get_reporting_status
        schema_ref: "media-buy/get-reporting-status-request.json"
        response_schema_ref: "media-buy/get-reporting-status-response.json"
        doc_ref: "/media-buy/task-reference/get_reporting_status"
        comply_scenario: reliable_reporting_reconciled_billing_probe
        stateful: true
        sample_request:
          account:
            account_id: "reporting_core_lab"
          view: "periods"
          changes_after: "$context.reliable_reporting_billing_checkpoint"
          context:
            correlation_id: "reliable_reporting_billing--checkpoint-repair"
        validations:
          - check: response_schema
            description: "Checkpoint repair response is schema-valid"
          - check: field_equals_context
            path: "revisions[0].reporting_revision_id"
            context_key: "reliable_reporting_billing_revision_id"
            description: "Original official invoice anchor remains unchanged"
          - check: field_value
            path: "periods[0].adjustment_count"
            value: 2
            description: "Both post-official corrections are enumerated"
          - check: array_length
            path: "adjustments"
            value: 2
            description: "Correction records converge independent of notifications"
          - check: array_length
            path: "adjustment_receipts"
            value: 2
            description: "Accepted and disputed outcomes both converge"
          - check: field_present
            path: "changes_checkpoint"
            description: "Buyer receives a new checkpoint after durable readback"

      - id: supersede_rejected_adjustment_receipt
        title: "Replace only the current rejected adjustment receipt"
        task: sync_reporting_receipts
        schema_ref: "media-buy/sync-reporting-receipts-request.json"
        response_schema_ref: "media-buy/sync-reporting-receipts-response.json"
        comply_scenario: reliable_reporting_reconciled_billing_probe
        stateful: true
        sample_request:
          account:
            account_id: "reporting_core_lab"
          idempotency_key: "$generate:uuid_v4#reliable_reporting_billing_adjustment_repair"
          adjustment_receipts:
            - reporting_receipt_id: "rr-adjustment-repaired-receipt-01"
              supersedes_reporting_receipt_id: "rr-adjustment-rejected-receipt-01"
              reporting_adjustment_id: "$context.reliable_reporting_disputed_adjustment_id"
              adjusts_reporting_revision_id: "$context.reliable_reporting_billing_revision_id"
              status: "accepted"
              observed_adjustment_sha256: "$context.reliable_reporting_disputed_adjustment_digest"
              observed_at: "2026-08-29T10:02:00Z"
        validations:
          - check: response_schema
            description: "Replacement acknowledgement is schema-valid"
          - check: field_value
            path: "results[0].result"
            value: "recorded"
            description: "A new immutable receipt supersedes the current rejected leaf"

      - id: reject_stale_adjustment_supersession
        title: "Reject supersession after the accepted current leaf becomes terminal"
        task: sync_reporting_receipts
        schema_ref: "media-buy/sync-reporting-receipts-request.json"
        response_schema_ref: "media-buy/sync-reporting-receipts-response.json"
        comply_scenario: reliable_reporting_reconciled_billing_probe
        stateful: true
        sample_request:
          account:
            account_id: "reporting_core_lab"
          idempotency_key: "$generate:uuid_v4#reliable_reporting_billing_stale_repair"
          adjustment_receipts:
            - reporting_receipt_id: "rr-adjustment-stale-receipt-01"
              supersedes_reporting_receipt_id: "rr-adjustment-repaired-receipt-01"
              reporting_adjustment_id: "$context.reliable_reporting_disputed_adjustment_id"
              adjusts_reporting_revision_id: "$context.reliable_reporting_billing_revision_id"
              status: "accepted"
              observed_adjustment_sha256: "$context.reliable_reporting_disputed_adjustment_digest"
              observed_at: "2026-08-29T10:03:00Z"
        validations:
          - check: response_schema
            description: "Terminal-leaf rejection response is schema-valid"
          - check: field_value
            path: "results[0].result"
            value: "failed"
            description: "Accepted current leaves cannot be superseded"

      - id: read_reconciliation_restored
        title: "Confirm current-leaf completion while retaining append-only history"
        task: get_reporting_status
        schema_ref: "media-buy/get-reporting-status-request.json"
        response_schema_ref: "media-buy/get-reporting-status-response.json"
        comply_scenario: reliable_reporting_reconciled_billing_probe
        stateful: true
        sample_request:
          account:
            account_id: "reporting_core_lab"
          view: "periods"
        validations:
          - check: response_schema
            description: "Restored reconciliation response is schema-valid"
          - check: field_value
            path: "periods[0].reconciliation_status"
            value: "accepted"
            description: "All current revision and adjustment leaves are accepted"
          - check: field_value
            path: "periods[0].health"
            value: "complete"
            description: "Accepted current leaves restore the closed fixture to complete"
          - check: field_value
            path: "periods[0].adjustment_receipt_count"
            value: 3
            description: "Immutable rejected and replacement receipt history remains countable"
